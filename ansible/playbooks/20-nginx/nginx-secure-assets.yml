---
- name: Generar cert + htpasswd para nginx-secure (se meten dentro de la imagen)
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - "{{ playbook_dir }}/../../group_vars/all/vm.yml"
    - "{{ playbook_dir }}/../../group_vars/vault.yml"
  roles:
    - nginx_secure_assets