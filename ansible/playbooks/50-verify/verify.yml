---
- name: Verificaciones post-despliegue (VM + AKS)
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - "{{ playbook_dir }}/../group_vars/all/terraform.yml"
    - "{{ playbook_dir }}/../group_vars/all/vm.yml"
    - "{{ playbook_dir }}/../group_vars/vault.yml"
    
  roles:
    - verify